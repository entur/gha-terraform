on:
  push

jobs:
  test-lint-ok:
    uses: ./.github/workflows/lint.yml
    with:
      terraform_dir: fixture/terraform

  test-terraform-plan:
    uses: ./.github/workflows/plan.yml
    with:
      terraform_dir: fixture/terraform
      secrets: inherit

